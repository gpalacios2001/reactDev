{
  "name": "backend",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=production node index.js",
    "dev": "NODE_ENV=development nodemon index.js",
    "test": "NODE_ENV=test node --test",
    "build:ui": "rm -rf dist && cd ../notes-frontend/ && npm run build && cp -r dist ../notes-backend",
    "deploy": "fly deploy",
    "deploy:full": "npm run build:ui && npm run deploy",
    "logs:prod": "fly logs"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gpalacios2001/LearningReact.git"
  },
  "author": "Gabe",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/gpalacios2001/LearningReact/issues"
  },
  "homepage": "https://github.com/gpalacios2001/LearningReact#readme",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-async-errors": "^3.1.1",
    "mongoose": "^8.4.1"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.5.7",
    "globals": "^15.3.0",
    "nodemon": "^3.1.2",
    "supertest": "^7.0.0"
  }
}
